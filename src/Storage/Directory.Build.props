<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)..\'))" />

  <PropertyGroup>
    <Product>$(Product) Storage</Product>
    <PackageTags>$(PackageTags) Storage</PackageTags>
  </PropertyGroup>

</Project>